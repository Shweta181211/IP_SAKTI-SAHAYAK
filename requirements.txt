pdfplumber>=0.11.0
pypdf>=5.0.0
pdf2image>=1.17.0
pytesseract>=0.3.10
sentence-transformers>=3.0.0
chromadb>=0.5.0

# --- Website (backend/main.py + frontend/) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0

# --- Optional LLM summaries ---
# The application works as a retrieval demo without an API key. This package
# is only used when the user deliberately enables LLM synthesis in the UI.
anthropic>=0.34.0
